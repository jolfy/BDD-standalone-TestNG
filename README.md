# Integration of Cucumber-JVM TestNG 

To see this whole thing running simply checkout this project and run this command:
mvn clean test


It runs two 'parallel' tests (setup using TestNG configuration)